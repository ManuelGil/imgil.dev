# ------------------------------------------------------
# Repository-scoped npm configuration
# ------------------------------------------------------
# This project uses npm as the only supported package manager.
# This file defines safe, explicit defaults for local and CI usage.
# ------------------------------------------------------

# Security
strict-ssl=true

# Determinism & hygiene
fund=false
audit=false
loglevel=warn

# Enforce npm usage
package-manager=npm
